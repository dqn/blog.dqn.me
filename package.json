{
  "name": "blog",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "yarn clean && yarn build && node --test",
    "watch": "yarn clean && tsc -w",
    "dev": "node ./dist/dev.js"
  },
  "devDependencies": {
    "@types/node": "^17.0.40",
    "typescript": "^4.7.3"
  }
}
