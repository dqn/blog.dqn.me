import { siteName } from "../siteName.js";
import type { Component } from "../types/Component.js";
import { h } from "./h.js";

const description = "おいで病院のミステリー";

type Props = {
  title: string;
  ogType: "website" | "article";
};

export const Layout: Component<Props> = ({ title, ogType }, ...children) => {
  return h(
    "html",
    { lang: "ja" },
    h(
      "head",
      {},
      h("meta", { name: "viewport", content: "width=device-width" }),
      h("meta", { charset: "utf-8" }),
      h("meta", { name: "theme-color", content: "#4c485b" }),
      h("title", {}, title),
      h("meta", { name: "description", content: description }),
      // style.css is generated by src/export.ts
      h("link", { rel: "preload", href: "/style.css", as: "style" }),
      h("link", { rel: "stylesheet", href: "/style.css" }),
      h("meta", { property: "og:type", content: ogType }),
      h("meta", { property: "og:url", content: "https://blog.dqn.me" }),
      h("meta", {
        property: "og:image",
        content: "https://blog.dqn.me/og.jpg",
      }),
      h("meta", { property: "og:title", content: title }),
      h("meta", { property: "og:site_name", content: siteName }),
      h("meta", { property: "og:description", content: description }),
      h("meta", { name: "twitter:card", content: "summary" }),
      h("meta", { name: "twitter:site", content: "dqn270" }),
      h("meta", { name: "twitter:creator", content: "dqn270" })
    ),
    h(
      "header",
      { class: "p-4 max-w-screen-md mx-auto" },
      h(
        "a",
        {
          href: "/",
          class: "text-xl text-white font-bold underline-none no-underline",
        },
        siteName
      )
    ),
    h("body", {}, ...children),
    h("footer", { class: "text-xs text-center my-4" }, "© 2023 dqn")
  );
};
